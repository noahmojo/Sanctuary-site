<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' — ' : '' %><%= settings.siteName %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
  <style>
    * { font-family: 'Inter', sans-serif; }
    html { scroll-behavior: smooth; }
    .fade-up { animation: fadeUp 0.8s ease-out forwards; opacity: 0; }
    @keyframes fadeUp { to { opacity: 1; transform: translateY(0); } from { opacity: 0; transform: translateY(20px); } }
    .hover-lift { transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1); }
    .hover-lift:hover { transform: translateY(-8px); box-shadow: 0 30px 60px -12px rgba(0,0,0,0.1); }
  </style>
</head>
<body class="bg-white text-neutral-900 antialiased">
  <nav class="fixed top-0 left-0 right-0 z-50 bg-white/90 backdrop-blur-xl border-b border-neutral-100">
    <div class="max-w-6xl mx-auto px-6">
      <div class="flex justify-between items-center h-16">
        <a href="/" class="text-lg font-medium tracking-tight"><%= settings.siteName %></a>
        <div class="hidden md:flex items-center gap-8">
          <a href="/animals" class="text-neutral-500 hover:text-neutral-900 transition text-sm">Animals</a>
          <a href="/book" class="text-neutral-500 hover:text-neutral-900 transition text-sm">Book a Visit</a>
          <a href="/about" class="text-neutral-500 hover:text-neutral-900 transition text-sm">About</a>
          <a href="/donate" class="bg-neutral-900 text-white px-5 py-2 rounded-full text-sm hover:bg-neutral-800 transition">Donate</a>
        </div>
      </div>
    </div>
  </nav>
  <main class="pt-16">
