<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %><%= settings.siteName %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
  <style>* { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-white text-neutral-800">
  <nav class="border-b border-neutral-100 px-6 py-4">
    <div class="max-w-6xl mx-auto flex justify-between items-center">
      <a href="/" class="flex items-center">
        <% if (settings.logo) { %>
          <img src="<%= settings.logo %>" alt="<%= settings.siteName %>" class="h-16 object-contain">
        <% } else { %>
          <span class="text-lg font-medium"><%= settings.siteName %></span>
        <% } %>
      </a>
      <div class="flex items-center gap-6 text-sm">
        <a href="/animals" class="text-neutral-600 hover:text-neutral-900 transition">Animals</a>
        <a href="/shop" class="text-neutral-600 hover:text-neutral-900 transition">Shop</a>
        <a href="/book" class="text-neutral-600 hover:text-neutral-900 transition">Book a Visit</a>
        <a href="/about" class="text-neutral-600 hover:text-neutral-900 transition">About</a>
        <a href="/cart" class="text-neutral-600 hover:text-neutral-900 transition relative">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"></path></svg>
          <% if (typeof cartCount !== 'undefined' && cartCount > 0) { %>
            <span class="absolute -top-2 -right-2 bg-emerald-600 text-white text-xs w-5 h-5 rounded-full flex items-center justify-center"><%= cartCount %></span>
          <% } %>
        </a>
        <a href="/donate" class="bg-neutral-900 text-white px-4 py-2 rounded-full text-sm hover:bg-neutral-700 transition">Donate</a>
      </div>
    </div>
  </nav>
  <main>
