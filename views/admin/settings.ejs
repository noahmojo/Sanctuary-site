<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Settings - Admin</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
  <style>* { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-neutral-50">
  <nav class="bg-white border-b border-neutral-200 px-6 py-4">
    <div class="max-w-6xl mx-auto flex justify-between items-center">
      <a href="/admin" class="font-medium">🦙 Sanctuary Admin</a>
      <div class="flex gap-6 text-sm">
        <a href="/admin/animals" class="text-neutral-600 hover:text-neutral-900">Animals</a>
        <a href="/admin/bookings" class="text-neutral-600 hover:text-neutral-900">Bookings</a>
        <a href="/admin/subscribers" class="text-neutral-600 hover:text-neutral-900">Subscribers</a>
        <a href="/admin/settings" class="text-neutral-900 font-medium">Settings</a>
        <a href="/admin/logout" class="text-neutral-600 hover:text-neutral-900">Logout</a>
      </div>
    </div>
  </nav>

  <main class="max-w-3xl mx-auto px-6 py-10">
    <h1 class="text-2xl font-light mb-8">Site Settings</h1>

    <form method="POST" action="/admin/settings" enctype="multipart/form-data" class="space-y-6">
      <div class="bg-white rounded-xl border border-neutral-200 p-6 space-y-5">
        <h2 class="font-medium text-lg">General</h2>
        
        <div>
          <label class="block text-sm font-medium mb-1.5">Site Name</label>
          <input type="text" name="siteName" value="<%= settings.siteName || '' %>" class="w-full bg-neutral-50 border border-neutral-200 rounded-lg px-4 py-2.5 text-sm">
        </div>
        
        <div>
          <label class="block text-sm font-medium mb-1.5">Header</label>
          <input type="text" name="header" value="<%= settings.header || '' %>" class="w-full bg-neutral-50 border border-neutral-200 rounded-lg px-4 py-2.5 text-sm">
        </div>
        
        <div>
          <label class="block text-sm font-medium mb-1.5">Subheader</label>
          <input type="text" name="subheader" value="<%= settings.subheader || '' %>" class="w-full bg-neutral-50 border border-neutral-200 rounded-lg px-4 py-2.5 text-sm">
        </div>
        
        <div>
          <label class="block text-sm font-medium mb-1.5">Tagline</label>
          <input type="text" name="tagline" value="<%= settings.tagline || '' %>" class="w-full bg-neutral-50 border border-neutral-200 rounded-lg px-4 py-2.5 text-sm">
        </div>
        
        <div>
          <label class="block text-sm font-medium mb-1.5">Location</label>
          <input type="text" name="location" value="<%= settings.location || '' %>" class="w-full bg-neutral-50 border border-neutral-200 rounded-lg px-4 py-2.5 text-sm">
        </div>
      </div>

      <div class="bg-white rounded-xl border border-neutral-200 p-6 space-y-5">
        <h2 class="font-medium text-lg">About</h2>
        
        <div>
          <label class="block text-sm font-medium mb-1.5">Short About (for footer)</label>
          <textarea name="about" rows="2" class="w-full bg-neutral-50 border border-neutral-200 rounded-lg px-4 py-2.5 text-sm resize-none"><%= settings.about || '' %></textarea>
        </div>
        
        <div>
          <label class="block text-sm font-medium mb-1.5">Full About Content</label>
          <textarea name="aboutContent" rows="6" class="w-full bg-neutral-50 border border-neutral-200 rounded-lg px-4 py-2.5 text-sm resize-none"><%= settings.aboutContent || '' %></textarea>
        </div>
      </div>

      <div class="bg-white rounded-xl border border-neutral-200 p-6 space-y-5">
        <h2 class="font-medium text-lg">Hero Image</h2>
        
        <% if (settings.heroImage) { %>
          <div class="mb-4">
            <img src="<%= settings.heroImage %>" alt="Current hero" class="w-48 h-32 object-cover rounded-lg">
            <input type="hidden" name="existingHeroImage" value="<%= settings.heroImage %>">
          </div>
        <% } %>
        
        <div>
          <label class="block text-sm font-medium mb-1.5">Upload New Image</label>
          <input type="file" name="heroImage" accept="image/*" class="w-full text-sm">
        </div>
      </div>

      <div class="bg-white rounded-xl border border-neutral-200 p-6 space-y-5">
        <h2 class="font-medium text-lg">Contact & Links</h2>
        
        <div>
          <label class="block text-sm font-medium mb-1.5">Email</label>
          <input type="email" name="email" value="<%= settings.email || '' %>" class="w-full bg-neutral-50 border border-neutral-200 rounded-lg px-4 py-2.5 text-sm">
        </div>
        
        <div>
          <label class="block text-sm font-medium mb-1.5">Phone</label>
          <input type="tel" name="phone" value="<%= settings.phone || '' %>" class="w-full bg-neutral-50 border border-neutral-200 rounded-lg px-4 py-2.5 text-sm">
        </div>
        
        <div>
          <label class="block text-sm font-medium mb-1.5">Donate URL</label>
          <input type="url" name="donateUrl" value="<%= settings.donateUrl || '' %>" placeholder="https://..." class="w-full bg-neutral-50 border border-neutral-200 rounded-lg px-4 py-2.5 text-sm">
        </div>
      </div>

      <div class="flex justify-end">
        <button type="submit" class="bg-emerald-600 text-white px-6 py-2.5 rounded-lg text-sm font-medium hover:bg-emerald-700 transition">Save Settings</button>
      </div>
    </form>
  </main>
</body>
</html>
