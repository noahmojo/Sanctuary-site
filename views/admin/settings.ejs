<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Subscribers - Admin</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
  <style>* { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="bg-neutral-50">
  <nav class="bg-white border-b border-neutral-200 px-6 py-4">
    <div class="max-w-6xl mx-auto flex justify-between items-center">
      <a href="/admin" class="font-medium">🦙 Sanctuary Admin</a>
      <div class="flex gap-6 text-sm">
        <a href="/admin/animals" class="text-neutral-600 hover:text-neutral-900">Animals</a>
        <a href="/admin/bookings" class="text-neutral-600 hover:text-neutral-900">Bookings</a>
        <a href="/admin/subscribers" class="text-neutral-900 font-medium">Subscribers</a>
        <a href="/admin/settings" class="text-neutral-600 hover:text-neutral-900">Settings</a>
        <a href="/admin/logout" class="text-neutral-600 hover:text-neutral-900">Logout</a>
      </div>
    </div>
  </nav>

  <main class="max-w-6xl mx-auto px-6 py-10">
    <div class="flex justify-between items-center mb-8">
      <div>
        <h1 class="text-2xl font-light">Subscribers</h1>
        <p class="text-neutral-500 text-sm mt-1"><%= subscribers.length %> total subscribers</p>
      </div>
      <a href="/admin/subscribers/export" class="bg-emerald-600 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-emerald-700 transition">Export CSV</a>
    </div>

    <div class="bg-white rounded-xl border border-neutral-200 overflow-hidden">
      <table class="w-full">
        <thead class="bg-neutral-50 border-b border-neutral-200">
          <tr>
            <th class="text-left px-6 py-3 text-sm font-medium text-neutral-600">Email</th>
            <th class="text-left px-6 py-3 text-sm font-medium text-neutral-600">Source</th>
            <th class="text-left px-6 py-3 text-sm font-medium text-neutral-600">Date</th>
            <th class="text-right px-6 py-3 text-sm font-medium text-neutral-600">Actions</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-neutral-100">
          <% if (subscribers.length === 0) { %>
            <tr>
              <td colspan="4" class="px-6 py-12 text-center text-neutral-400">No subscribers yet</td>
            </tr>
          <% } %>
          <% subscribers.forEach(sub => { %>
            <tr class="hover:bg-neutral-50">
              <td class="px-6 py-4 text-sm"><%= sub.email %></td>
              <td class="px-6 py-4 text-sm">
                <span class="inline-block px-2 py-1 rounded-full text-xs <%= sub.source === 'booking' ? 'bg-blue-50 text-blue-600' : 'bg-emerald-50 text-emerald-600' %>">
                  <%= sub.source %>
                </span>
              </td>
              <td class="px-6 py-4 text-sm text-neutral-500"><%= new Date(sub.created_at).toLocaleDateString() %></td>
              <td class="px-6 py-4 text-right">
                <form method="POST" action="/admin/subscribers/<%= sub.id %>/delete" class="inline" onsubmit="return confirm('Remove this subscriber?')">
                  <button type="submit" class="text-red-500 hover:text-red-700 text-sm">Remove</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </main>
</body>
</html>
